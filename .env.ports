# Master Port Configuration
# Change these values to update all services across the application
# After changing, run: scripts\deploy.bat

# Service Ports (exposed to host)
FRONTEND_PORT=8086
BACKEND_PORT=9200
DATABASE_PORT=5433

# Container Internal Ports (rarely need to change)
BACKEND_CONTAINER_PORT=8000
FRONTEND_CONTAINER_PORT=80
DATABASE_CONTAINER_PORT=5432

# Constructed URLs (automatically built from ports above)
FRONTEND_URL=http://localhost:8086
API_URL=http://localhost:9200
WS_URL=ws://localhost:9200

# CORS Origins (for backend to accept frontend requests)
CORS_ORIGINS=http://localhost:8086,http://127.0.0.1:8086,http://localhost:5173,http://localhost:5174,https://taskmanager.sulemankhan.me